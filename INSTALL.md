# Instructions for the installing AI

Install and start this skill for the person; do not turn this file into a manual questionnaire or a list of shell commands for them.

1. Detect the current harness, project, file permissions, installed skill and usable runtimes. Read the repository before executing its helpers. Do not inspect unrelated personal projects. Reuse an existing project skill if its release matches. Before either installation method below, preserve any differing installed copy and inspect its differences. Obtain update authorization if it is not already provided; never silently overwrite local customizations.
2. Prefer project-scoped installation. With Node >=22.20, run `npx skills@1.5.26 add toughdave/job-search --skill job-search --agent codex --copy -y` for Codex, or replace `codex` with `claude-code`. Install both only when requested. Never install globally by default. The user may need to approve network access or installation under their harness policy.
3. Without a compatible Node, download a tagged release or a temporary shallow Git checkout, and copy the **entire** `skills/job-search` directory into `.agents/skills/job-search` (Codex) or `.claude/skills/job-search` (Claude Code) inside the selected project. Never copy the candidate's resume into that installed directory. Do not pipe uninspected remote text into a shell. Refuse to replace a differing existing install silently: preserve it, inspect differences, and confirm an update if required.
4. Open the installed SKILL.md and check that its relative references, `scripts/workspace.py`, `scripts/documents.py`, and document assets exist. Use the harness's skill listing/selection to verify discovery when available. A copied file alone is not evidence that discovery worked. If a fresh turn/restart is required, explain that single action; never claim to have activated an unavailable skill.
5. Begin onboarding with the supplied resume. Let the skill initialize a NEW private workspace. Keep it outside this repository and outside any installed skill directory. A new empty sibling directory is a useful proposal. If write permissions prevent this, ask for one writable location. Never reuse an unrelated existing job pipeline. No global memory, personal skill, account connection, permission mode or schedule is changed by installation.

## Updates

Use the same project and explicitly update only this skill. Preserve any locally changed installed version before replacement. The distribution directory must contain no private workspace. Check the new version's state schema support before use; v1 refuses unsupported schemas rather than migrating destructively. Reopen the existing private workspace and verify its workspace ID, revision and current next action. Do not initialize again. No update may overwrite profile, Q&A, applications or files.
